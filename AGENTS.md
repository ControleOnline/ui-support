## Escopo
- Modulo de suporte.
- A implementacao encontrada aqui hoje esta em Vue legado com store proprio.

## Estado
- Este modulo hoje nao tem `src/react`; a implementacao disponivel fica em `src/vue` com store proprio.
- Em novos prompts, priorizar modulos React equivalentes quando existirem.
- So mexer aqui se o pedido for explicitamente sobre esse fluxo antigo ou se nao houver alternativa atual.

## Quando usar
- Prompts sobre suporte, atendimento interno e telas antigas dessa area.
